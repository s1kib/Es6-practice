// when the value is fixed we must declare Const keyword as a variable
const hubby = "Elias Kanchon";

// console.log(hubby);

// When our value needs to change we should declare Let keyword as a variable
let patientName = "sabbir vai";
patientName = "Rahima Khala";
patientName = "Baker vai";
// console.log(patientName)

// we can change an index from an array we can push and pop but we cant change the array we declare
const numbers = [12, 23, 32, 42];
// numbers[2] = 45;
// numbers.push(34);
// numbers.pop(2);
// numbers = ["sakib", "safi", "sakib"]
// console.log(numbers);
let  sum = 0;
for(let i = 0; i < 10; i++){
    sum = sum + i;
}
console.log(sum);

